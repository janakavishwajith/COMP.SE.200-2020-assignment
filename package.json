{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "mocha --timeout 10000 test/*",
    "c8:report": "c8 --include src --all --exclude src/.internal mocha && c8 report --include src --all --exclude src/.internal --reporter=lcov",
    "coveralls": "cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.2.1"
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "mocha-lcov-reporter": "^1.3.0",
    "c8": "^7.3.5"
  }
}
