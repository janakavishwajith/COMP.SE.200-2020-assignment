language: node_js
node_js:
  - "14.15.0"
  - "12.18.0"

install: 
  - npm ci

script: 
  - npm run test
  - npm run c8:report
  - npm run coveralls

after_script:
  addons:
    artifacts:
      paths:
        - $HOME/mochawesome-report/*
      debug: true